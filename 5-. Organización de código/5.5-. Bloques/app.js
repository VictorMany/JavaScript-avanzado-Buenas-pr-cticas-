var estado = true;

//Si solo hay una indicacion no es necesario poner llaves
if (estado)
    console.log('Avanzar')

//Si hay mas de una indicacion es necesario poner llaves
if (estado) {
    console.log('Avanzar');
    var resultado = 2 + 2;
}

